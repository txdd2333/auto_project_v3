# OceanBase MySQL 租户配置
# 使用方式：cp .env.mysql .env

VITE_SERVICE_PROVIDER=custom

# API 服务器地址（前端访问后端） - 使用部署服务器地址
VITE_API_URL=http://192.168.1.2:3000

# Playwright 服务器地址（前端访问 Playwright 自动化服务）
VITE_PLAYWRIGHT_URL=http://192.168.1.2:3001

# OceanBase MySQL 租户数据库配置
# 连接串格式: mysql -h{HOST} -P{PORT} -u{USER}@{TENANT}#{CLUSTER} -p{PASSWORD}
DB_HOST=192.168.1.70
DB_PORT=2883
DB_USER="root@Tianji4_MySQL#Tianji4"
DB_PASSWORD=aaAA11__
DB_DATABASE=ops_workflow_center

# JWT 密钥（生产环境必须修改！）
JWT_SECRET=change-this-to-a-random-secret-key-in-production-environment

# 文件上传目录（相对于项目根目录）
UPLOAD_DIR=./uploads

# OceanBase 说明：
# - 用户名格式: user@tenant#cluster（必须完整保留，包括@和#符号）
# - OceanBase 兼容 MySQL 5.7/8.0 协议
# - 使用 mysql2 客户端库可直接连接
# - 需要确保 ops_workflow_center 数据库已创建
#
# 启动步骤：
# 1. npm install          # 安装依赖
# 2. npm run build        # 构建项目
# 3. npm run server       # 启动 API 服务器（终端 1）
# 4. npm run api-server   # 启动 Playwright 服务器（终端 2）
# 5. npm run dev          # 启动前端开发服务器（终端 3）
#
# 或使用 PM2 生产部署：
# pm2 start dist/server/index.js --name "ops-api"
# pm2 start dist/server/api-server.js --name "ops-playwright"

# Supabase 占位符（使用 MySQL 模式时不需要真实值）
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-key
